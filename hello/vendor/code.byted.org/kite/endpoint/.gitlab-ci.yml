image: golang

before_script:
  - mkdir -p $GOPATH/src/code.byted.org/kite
  - cp -r $CI_PROJECT_DIR $GOPATH/src/code.byted.org/kite/endpoint
  - cd $GOPATH/src/code.byted.org/kite/endpoint
  - go get -d ./

test:
  script:
  - go test -v -cover -bench ./
